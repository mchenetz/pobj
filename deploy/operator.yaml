apiVersion: v1
kind: Namespace
metadata:
  name: entity-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: entity-operator
  namespace: entity-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: entity-operator
  template:
    metadata:
      labels:
        app: entity-operator
    spec:
      serviceAccountName: entity-operator
      containers:
      - name: operator
        image: ghcr.io/mchenetz/entity:latest
        imagePullPolicy: IfNotPresent
        command: ["/entity-operator"]
        env:
        - name: ENTITY_IMAGE
          value: ghcr.io/mchenetz/entity:latest
        ports:
        - containerPort: 8080
          name: metrics
        - containerPort: 8081
          name: probes
        livenessProbe:
          httpGet:
            path: /healthz
            port: probes
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: probes
          initialDelaySeconds: 5
          periodSeconds: 10
